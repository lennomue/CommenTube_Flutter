# commmit毎の変更内容をメモ
以下の内容はAIまたは人手で追記していく

## 2026-09-17: フェーズ0 開発環境・雛形構築

- Riverpod、go_router、Drift、WebViewの依存関係を追加
- 廃止済みの直接依存`sqlite3_flutter_libs`を、Drift公式推奨の`drift_flutter`と`path_provider`へ変更
- Flutter 3.44.9との互換性のため`go_router`を17.5系に固定
- home/game/result/libraryのルートと機能別ディレクトリを追加
- HomeとLibraryを往復できる共通ボトムナビゲーションを追加
- Home画面の既存UIを機能別ファイルへ分離
- 静的解析、ウィジェットテスト、iOS向け署名なしデバッグビルドの成功を確認

## 2026-09-17: フェーズ1 JSON固定データによる画面実装

- 仕様の`quizzes`定義に沿った5問分の固定データと、YouTube Data API形式を模した統計モックを追加
- 将来のSupabase・実API移行でも画面側の呼び出しを変えない`QuizRepository`インターフェースを追加
- クイズ本体、動画統計、コメント高評価数を`QuizWithLiveStats`へ結合する処理を実装
- Homeに代表コメント・再生数・公開からの経過期間を表示するスクロールフィードを実装
- Gameに固定統計と全開放状態のコメント・歌詞・楽曲情報ヒントを実装
- Resultに楽曲の全情報、次の問題、Homeへ戻る導線を実装
- クイズIDをパスに含むHome→Game→Result→Homeの画面遷移を実装
- 320×568相当の小型画面でもHomeがオーバーフローしないレスポンシブ表示へ調整
- JSON結合テスト、画面遷移テスト、静的解析、iOS向け署名なしデバッグビルドの成功を確認

## 2026-09-18: フェーズ2 WebView回答機構の実装

- Flutter製のヘッダー・ナビゲーション・回答ボタンを持つアプリ内YouTube WebViewを追加
- YouTubeホームを空の検索状態で開き、URLの変化から`watch?v=`形式の動画IDを抽出する処理を追加
- iOSのインライン動画再生を有効化し、初回自動再生と意図しないフルスクリーン再生を抑止
- HTTP/HTTPS以外の遷移を遮断し、YouTubeアプリなど外部アプリへの遷移を防止
- ANSWERタップ時に動画IDをクイズの正解IDと比較する正誤判定を実装
- 正解時はWebViewを閉じてResultへ、不正解時はGameへ戻る導線を実装
- WebViewを閉じる際に動画を停止し、空ページへ遷移して再生を終了する処理を追加
- URL抽出・正誤判定テストを追加し、全8テスト、静的解析、iOSビルドの成功を確認
- iPhone実機でWebView内のURL追跡・動画ID検出・回答導線が動作することを確認

## 2026-09-18: 仕様書・開発計画の更新

- Homeの簡易タグ表示と詳細絞り込みUIの方針を`spec.md`へ追記
- Gameのヒントタブ、スクロールに応じた回答UI、Resultのお気に入り操作を仕様化
- Libraryのお気に入り分類と楽曲詳細表示の仕様を具体化
- 開発計画へLibrary実装とデザイン向上のフェーズを追加し、外部API接続をフェーズ7へ移動
- 各コミット後にGitHubへプッシュする進行ルールを`plan.md`へ追加
- 現行の`spec.md`へ仕様を統合したため、旧モック仕様書`spec_mock.md`を削除

## 2026-09-18: フェーズ3 ヒント開示機構の実装

- Game画面のヒントをコメント・歌詞・楽曲情報の3タブに分類
- Homeのサムネイルに使う代表コメントを、クイズ開始時から開放済みで表示
- 代表コメント以外の各ヒントに、タップすると即時表示される開放ボタンを追加
- ヒント開放状態は1回のプレイ中のメモリ上のみで保持し、履歴を永続化しない設計に変更
- Homeへ戻って同じクイズを開き直すと、代表コメント以外が未開放に戻るテストを追加
- `spec.md`、`spec_mock.md`、`plan.md`を新しい開放仕様に合わせて更新
- 全8テスト、静的解析、iOS向け署名なしデバッグビルドの成功を確認

## 2026-09-18: フェーズ3.1 画面遷移とヒント表示の改善

- HomeとLibraryを`StatefulShellRoute.indexedStack`配下にまとめ、共通フッターを維持したままタブを切り替える構成に変更
- HomeとLibraryの各画面状態をタブ切り替え後も保持する構成に変更
- HomeからGameへは履歴に積み、GameのHomeボタンでは履歴を戻ることで、進む時と逆方向の画面遷移を実装
- 未開放ヒントの「〜は未開放です」を廃止し、鍵アイコンと項目名だけの表示に簡素化
- フッターがHome・Library間で同じ要素として保持されるウィジェットテストを追加
- `plan.md`にフェーズ3.1とResultの共有ボタン計画、`spec.md`に段階的なヒント開示の展望を追加
- 全8テストと静的解析の成功、iPhone実機で自然な画面遷移と表示になったことを確認

## 2026-09-18: フェーズ4 Homeの絞り込みUXの実装

- Home上部にJ-Pop・ロック・英語・日本語・2010年代のクイックフィルターを追加
- 楽曲ジャンル・言語・動画ジャンル・投稿年範囲を指定できる詳細絞り込みシートを追加
- 絞り込み条件が0件の場合の案内と、条件クリア導線を追加
- 将来のSupabase RPCへの置き換え後も画面側の呼び出しを保てる`QuizFilter`とRepositoryインターフェースを追加
- JSONモック期間限定の`.where()`絞り込みをRepository内に実装し、Supabase移行予定をコメントで明記
- 複数カテゴリの複合条件、選択・解除、詳細シートの表示をテストに追加
- Libraryメイン画面のデザイン方針と、画像生成より標準UI・プレースホルダーを優先する方針を`plan.md`へ追加
- 全9テストと静的解析の成功を確認
